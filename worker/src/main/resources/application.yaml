app:
  polyglot_book: ${POLYGLOT_BOOK}
  syzygy_directory: ${SYZYGY_DIRECTORY}

spring:
  application:
    name: sosa-worker

  main:
    web-application-type: none

  rabbitmq:
    host: ${RABBIT_HOST}
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    # Recommended for publishers:
    publisher-confirm-type: correlated
    publisher-returns: true
